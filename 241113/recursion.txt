Bubble Sort (*버블 정렬)
- 특정 배열안에 앞에 있는 값과 뒤에 있는 값을 연이어서 비교해서 최종적으로
배열 내 값을 정렬하는 것
- 

Select Sort (*선택 정렬)
- 특정 배열안에서 순차적으로 값을 선택해서 배열의 모든 값들과 비교 후
 최종적으로 배열 내 값을 정렬하는 것

자료구조 & 알고리즘
> 지금 당장 이것을 배웠다고 해서 실력이 급작스럽게 x
> 어떻게 데이터를 정렬 // 관리 // 사용할 수 있는지에 대한 인사니트를 얻는 다는 생각으로 공부